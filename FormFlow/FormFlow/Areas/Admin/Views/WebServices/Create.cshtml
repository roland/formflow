@model FormFlow.Models.WebService

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>WebService</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.WsdlUrl)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.WsdlUrl)
            @Html.ValidationMessageFor(model => model.WsdlUrl)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.CachedWsdl)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.CachedWsdl)
            @Html.ValidationMessageFor(model => model.CachedWsdl)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
